<!--components/scenes/title-scene/title-scene.wxml — 启动页 (V0.48)
  背景完全透明 → 全局 bg-fx 糖果动态背景透出 (数字 1-9 + 3×3/4×4/5×5/6×6 网格 + 呼吸)
  不再显示中央 9×9 封面框 (V0.48 移除), 中间留空让背景氛围铺满
  本组件只承载前景 UI:
    - brand     顶部品牌标题 (阴影保证浅色背景可读)
    - press     Press START 糖果色胶囊 (脉冲呼吸, 中部偏下)
    - copyright 版权两行
  全屏点击 → onTapStart → triggerEvent('start')
-->
<view class="title-page" bind:tap="onTapStart">

  <!-- 点击捕获层 (V0.53.3: tap 直接绑本层 — 对齐 menu-scene 每个实色 view 直绑 bindtap 的成功模式.
       V0.53 之前仅靠"透明层承接 → 冒泡到根 bindtap", Skyline 下透明命中 + 组件根冒泡两条都不可靠 →
       "看到但点了没反应". 现在该层有微透明背景 (wxss rgba .01, 视觉无差) 保证命中测试认可,
       且自身直绑 onTapStart, 不再依赖冒泡.
       同时它给 title-page 提供 in-flow 内容撑高, 兜底宿主高度计算异常导致的塌陷. -->
  <view class="tap-catcher" bind:tap="onTapStart"></view>

  <!-- 前景: 品牌标题 (V0.49.6 重新起名: 主玩法图画谜题 → 拼豆图纸闭环) -->
  <view class="brand-wrap">
    <view class="brand-en">点豆成画</view>
    <view class="brand-sub">PUZZLE · PICTURE · BEADS</view>
  </view>

  <!-- Press START (V0.48: 原中央封面删掉, press 上移到中部留白区)
       V0.53.3: press-start 胶囊本身也直绑 onTapStart — 实色节点命中最可靠,
       即使用户点在胶囊上且冒泡被 Skyline 打断也能直接触发. -->
  <view class="press-area">
    <view class="press-start {{pulse ? 'pulse' : ''}}" bind:tap="onTapStart">
      <text class="press-start-text">Press START</text>
    </view>
    <view class="hint-line">TAP ANYWHERE TO BEGIN</view>
  </view>

  <!-- 版权 -->
  <view class="copyright">
    <text class="copyright-text">© 2006 DIGITALWARE.</text>
    <text class="copyright-text">© 2006 D3 Publisher. All Rights Reserved.</text>
  </view>

</view>
