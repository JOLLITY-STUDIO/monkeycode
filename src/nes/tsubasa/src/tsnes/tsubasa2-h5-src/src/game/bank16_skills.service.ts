/**
 * Bank 16 Service — Special Moves & Skills (骨架)
 *
 * CPU 映射: $8000-$9FFF (MMC3 R6 切换, Bank #0x10 = 16)
 * PRG offset: 0x020010-0x02200F
 *
 * H5 版本: 无 MMC3 / CPU 模拟。数据直接使用 `rom-data/prg-bank-16.ts` 原始字节,
 *           本文件为 code 翻译骨架 (来源: _tmp_bzk_out/bank_16.asm, CDL C 标记)。
 *
 * 原始入口 (跳转表):
 *   [0] $8021
 *
 * code 段 (18):
 *   $8003-$80AC (170 B)
 *   $80D1-$8170 (160 B)
 *   $8207-$8290 (138 B)
 *   $829A-$82FB (98 B)
 *   $8330-$838E (95 B)
 *   $839B-$83AE (20 B)
 *   $83B7-$83BA (4 B)
 *   $83C5-$83D5 (17 B)
 *   $83E7-$8441 (91 B)
 *   $8451-$8579 (297 B)
 *   $8583-$8634 (178 B)
 *   $863E-$86A5 (104 B)
 *   $86B8-$86E0 (41 B)
 *   $87E2-$886A (137 B)
 *   $8879-$88F1 (121 B)
 *   $8910-$893A (43 B)
 *   $8944-$89AA (103 B)
 *   $89B3-$89B3 (1 B)
 *
 * 本地函数 (被 JSR 调用, 13):
 *   $80A9×1 $8138×3 $8150×2 $816E×1 $8207×22 $8211×8 $82FB×1 $835C×1 $8677×1 $886A×1 $8991×1 $899C×1 $89A7×1
 *
 * 本地 JMP 目标:
 *   $8021×1 $803F×1 $80CF×1 $80F6×2 $812F×2 $8142×1 $8211×2
 */

import { DataStore } from '../data/DataStore';
import PRG_BANK_16 from "../../../rom-data/prg-bank-16";

// ═══════════════════════════════════════════════════════════════
// Bank16Service
// ═══════════════════════════════════════════════════════════════

export class Bank16Service {
  constructor(private _store: DataStore) {}

  // ── 数据访问 (原始字节, 未结构化) ──

  /** 读取本 bank 内地址 addr 的原始字节 (addr: $8003-$89B3) */
  readByte(addr: number): number {
    return PRG_BANK_16[addr - 0x8000] ?? 0xFF;
  }

  /** 读取本 bank 内 16bit 小端数值 */
  readU16(addr: number): number {
    return this.readByte(addr) | (this.readByte(addr + 1) << 8);
  }

  get store(): DataStore { return this._store; }

  // ──────────────────────────────────────────────
  // $8003: 入口跳转表 (1 路)
  // ──────────────────────────────────────────────

  /**
   * 跳转表分发 (bank_16.asm $8003)
   *   [0]→$8021
   */
  dispatch(index: number): void {
    switch (index) {
      case 0: this.entry_8021(); break;
      default: break;
    }
  }

  // ──────────────────────────────────────────────
  // $8021: 入口
  // ──────────────────────────────────────────────

  /**
   * $8021 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8021(): void {
    // TODO: 翻译自 bank_16.asm $8021
  }

  // ──────────────────────────────────────────────
  // $80A9: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $80A9 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_80A9(): void {
    // TODO: 翻译自 bank_16.asm $80A9
  }

  // ──────────────────────────────────────────────
  // $80D1: 入口
  // ──────────────────────────────────────────────

  /**
   * $80D1 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_80D1(): void {
    // TODO: 翻译自 bank_16.asm $80D1
  }

  // ──────────────────────────────────────────────
  // $8138: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $8138 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_8138(): void {
    // TODO: 翻译自 bank_16.asm $8138
  }

  // ──────────────────────────────────────────────
  // $8150: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $8150 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_8150(): void {
    // TODO: 翻译自 bank_16.asm $8150
  }

  // ──────────────────────────────────────────────
  // $816E: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $816E — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_816E(): void {
    // TODO: 翻译自 bank_16.asm $816E
  }

  // ──────────────────────────────────────────────
  // $8207: 入口
  // ──────────────────────────────────────────────

  /**
   * $8207 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8207(): void {
    // TODO: 翻译自 bank_16.asm $8207
  }

  // ──────────────────────────────────────────────
  // $8211: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $8211 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_8211(): void {
    // TODO: 翻译自 bank_16.asm $8211
  }

  // ──────────────────────────────────────────────
  // $829A: 入口
  // ──────────────────────────────────────────────

  /**
   * $829A — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_829A(): void {
    // TODO: 翻译自 bank_16.asm $829A
  }

  // ──────────────────────────────────────────────
  // $82FB: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $82FB — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_82FB(): void {
    // TODO: 翻译自 bank_16.asm $82FB
  }

  // ──────────────────────────────────────────────
  // $8330: 入口
  // ──────────────────────────────────────────────

  /**
   * $8330 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8330(): void {
    // TODO: 翻译自 bank_16.asm $8330
  }

  // ──────────────────────────────────────────────
  // $835C: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $835C — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_835C(): void {
    // TODO: 翻译自 bank_16.asm $835C
  }

  // ──────────────────────────────────────────────
  // $839B: 入口
  // ──────────────────────────────────────────────

  /**
   * $839B — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_839B(): void {
    // TODO: 翻译自 bank_16.asm $839B
  }

  // ──────────────────────────────────────────────
  // $83B7: 入口
  // ──────────────────────────────────────────────

  /**
   * $83B7 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_83B7(): void {
    // TODO: 翻译自 bank_16.asm $83B7
  }

  // ──────────────────────────────────────────────
  // $83C5: 入口
  // ──────────────────────────────────────────────

  /**
   * $83C5 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_83C5(): void {
    // TODO: 翻译自 bank_16.asm $83C5
  }

  // ──────────────────────────────────────────────
  // $83E7: 入口
  // ──────────────────────────────────────────────

  /**
   * $83E7 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_83E7(): void {
    // TODO: 翻译自 bank_16.asm $83E7
  }

  // ──────────────────────────────────────────────
  // $8451: 入口
  // ──────────────────────────────────────────────

  /**
   * $8451 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8451(): void {
    // TODO: 翻译自 bank_16.asm $8451
  }

  // ──────────────────────────────────────────────
  // $8583: 入口
  // ──────────────────────────────────────────────

  /**
   * $8583 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8583(): void {
    // TODO: 翻译自 bank_16.asm $8583
  }

  // ──────────────────────────────────────────────
  // $863E: 入口
  // ──────────────────────────────────────────────

  /**
   * $863E — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_863E(): void {
    // TODO: 翻译自 bank_16.asm $863E
  }

  // ──────────────────────────────────────────────
  // $8677: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $8677 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_8677(): void {
    // TODO: 翻译自 bank_16.asm $8677
  }

  // ──────────────────────────────────────────────
  // $86B8: 入口
  // ──────────────────────────────────────────────

  /**
   * $86B8 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_86B8(): void {
    // TODO: 翻译自 bank_16.asm $86B8
  }

  // ──────────────────────────────────────────────
  // $87E2: 入口
  // ──────────────────────────────────────────────

  /**
   * $87E2 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_87E2(): void {
    // TODO: 翻译自 bank_16.asm $87E2
  }

  // ──────────────────────────────────────────────
  // $886A: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $886A — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_886A(): void {
    // TODO: 翻译自 bank_16.asm $886A
  }

  // ──────────────────────────────────────────────
  // $8879: 入口
  // ──────────────────────────────────────────────

  /**
   * $8879 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8879(): void {
    // TODO: 翻译自 bank_16.asm $8879
  }

  // ──────────────────────────────────────────────
  // $8910: 入口
  // ──────────────────────────────────────────────

  /**
   * $8910 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8910(): void {
    // TODO: 翻译自 bank_16.asm $8910
  }

  // ──────────────────────────────────────────────
  // $8944: 入口
  // ──────────────────────────────────────────────

  /**
   * $8944 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_8944(): void {
    // TODO: 翻译自 bank_16.asm $8944
  }

  // ──────────────────────────────────────────────
  // $8991: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $8991 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_8991(): void {
    // TODO: 翻译自 bank_16.asm $8991
  }

  // ──────────────────────────────────────────────
  // $899C: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $899C — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_899C(): void {
    // TODO: 翻译自 bank_16.asm $899C
  }

  // ──────────────────────────────────────────────
  // $89A7: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $89A7 — 内部函数 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  private fn_89A7(): void {
    // TODO: 翻译自 bank_16.asm $89A7
  }

  // ──────────────────────────────────────────────
  // $89B3: 入口
  // ──────────────────────────────────────────────

  /**
   * $89B3 — 入口 (TODO 翻译)
   * 原始: bank_16.asm, CDL code 标记
   */
  entry_89B3(): void {
    // TODO: 翻译自 bank_16.asm $89B3
  }

}