/**
 * Bank 11 Service — Match Turn Logic (PT1) (骨架)
 *
 * 数据已直接 import `rom-data/prg-bank-11.ts` (Bank #0x0B = 11), 无 MMC3 切换。
 * PRG offset: 0x016010-0x01800F
 *
 * 本文件为 code 翻译骨架 (来源: _tmp_bzk_out/bank_11.asm, CDL C 标记)。
 *
 * 原始入口 (跳转表):
 *   [0] $8083
 *   [1] $84A1
 *   [2] $814C
 *
 * code 段 (8):
 *   $8003-$81A7 (421 B)
 *   $81BC-$81C3 (8 B)
 *   $81CF-$81D2 (4 B)
 *   $8250-$8273 (36 B)
 *   $827C-$827C (1 B)
 *   $82F7-$83FC (262 B)
 *   $840A-$8463 (90 B)
 *   $8471-$86ED (637 B)
 *
 * 本地函数 (被 JSR 调用, 13):
 *   $810C×2 $812B×1 $81A7×1 $81BC×1 $82FE×2 $845C×1 $847F×2 $84A1×2 $84CF×2 $84F4×2 $8525×7 $85C2×3 $86D3×2
 *
 * 本地 JMP 目标:
 *   $800C×2 $8083×3 $814C×1 $819C×1 $8279×1 $8329×1 $832B×2 $8386×1 $840D×1 $8493×2 $84A1×1 $856D×1 $86DF×1
 */

import { DataStore } from '../data/DataStore';
import PRG_BANK_11 from "../../../rom-data/prg-bank-11";

// ═══════════════════════════════════════════════════════════════
// Bank11Service
// ═══════════════════════════════════════════════════════════════

export class Bank11Service {
  constructor(private _store: DataStore) {}

  // ── 数据访问 (原始字节, 未结构化) ──

  /** 读取本 bank 内地址 addr 的原始字节 (addr: $8003-$86ED) */
  readByte(addr: number): number {
    return PRG_BANK_11[addr - 0x8000] ?? 0xFF;
  }

  /** 读取本 bank 内 16bit 小端数值 */
  readU16(addr: number): number {
    return this.readByte(addr) | (this.readByte(addr + 1) << 8);
  }

  get store(): DataStore { return this._store; }

  // ──────────────────────────────────────────────
  // $8003: 入口跳转表 (3 路)
  // ──────────────────────────────────────────────

  /**
   * 跳转表分发 (bank_11.asm $8003)
   *   [0]→$8083  [1]→$84A1  [2]→$814C
   */
  dispatch(index: number): void {
    switch (index) {
      case 0: this.entry_8083(); break;
      case 1: this.entry_84A1(); break;
      case 2: this.entry_814C(); break;
      default: break;
    }
  }

  // ──────────────────────────────────────────────
  // $8083: 入口
  // ──────────────────────────────────────────────

  /**
   * $8083 — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_8083(): void {
    // TODO: 翻译自 bank_11.asm $8083
  }

  // ──────────────────────────────────────────────
  // $810C: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $810C — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_810C(): void {
    // TODO: 翻译自 bank_11.asm $810C
  }

  // ──────────────────────────────────────────────
  // $812B: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $812B — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_812B(): void {
    // TODO: 翻译自 bank_11.asm $812B
  }

  // ──────────────────────────────────────────────
  // $814C: 入口
  // ──────────────────────────────────────────────

  /**
   * $814C — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_814C(): void {
    // TODO: 翻译自 bank_11.asm $814C
  }

  // ──────────────────────────────────────────────
  // $81A7: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $81A7 — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_81A7(): void {
    // TODO: 翻译自 bank_11.asm $81A7
  }

  // ──────────────────────────────────────────────
  // $81BC: 入口
  // ──────────────────────────────────────────────

  /**
   * $81BC — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_81BC(): void {
    // TODO: 翻译自 bank_11.asm $81BC
  }

  // ──────────────────────────────────────────────
  // $81CF: 入口
  // ──────────────────────────────────────────────

  /**
   * $81CF — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_81CF(): void {
    // TODO: 翻译自 bank_11.asm $81CF
  }

  // ──────────────────────────────────────────────
  // $8250: 入口
  // ──────────────────────────────────────────────

  /**
   * $8250 — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_8250(): void {
    // TODO: 翻译自 bank_11.asm $8250
  }

  // ──────────────────────────────────────────────
  // $827C: 入口
  // ──────────────────────────────────────────────

  /**
   * $827C — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_827C(): void {
    // TODO: 翻译自 bank_11.asm $827C
  }

  // ──────────────────────────────────────────────
  // $82F7: 入口
  // ──────────────────────────────────────────────

  /**
   * $82F7 — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_82F7(): void {
    // TODO: 翻译自 bank_11.asm $82F7
  }

  // ──────────────────────────────────────────────
  // $82FE: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $82FE — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_82FE(): void {
    // TODO: 翻译自 bank_11.asm $82FE
  }

  // ──────────────────────────────────────────────
  // $840A: 入口
  // ──────────────────────────────────────────────

  /**
   * $840A — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_840A(): void {
    // TODO: 翻译自 bank_11.asm $840A
  }

  // ──────────────────────────────────────────────
  // $845C: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $845C — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_845C(): void {
    // TODO: 翻译自 bank_11.asm $845C
  }

  // ──────────────────────────────────────────────
  // $8471: 入口
  // ──────────────────────────────────────────────

  /**
   * $8471 — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_8471(): void {
    // TODO: 翻译自 bank_11.asm $8471
  }

  // ──────────────────────────────────────────────
  // $847F: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $847F — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_847F(): void {
    // TODO: 翻译自 bank_11.asm $847F
  }

  // ──────────────────────────────────────────────
  // $84A1: 入口
  // ──────────────────────────────────────────────

  /**
   * $84A1 — 入口 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  entry_84A1(): void {
    // TODO: 翻译自 bank_11.asm $84A1
  }

  // ──────────────────────────────────────────────
  // $84CF: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $84CF — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_84CF(): void {
    // TODO: 翻译自 bank_11.asm $84CF
  }

  // ──────────────────────────────────────────────
  // $84F4: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $84F4 — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_84F4(): void {
    // TODO: 翻译自 bank_11.asm $84F4
  }

  // ──────────────────────────────────────────────
  // $8525: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $8525 — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_8525(): void {
    // TODO: 翻译自 bank_11.asm $8525
  }

  // ──────────────────────────────────────────────
  // $85C2: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $85C2 — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_85C2(): void {
    // TODO: 翻译自 bank_11.asm $85C2
  }

  // ──────────────────────────────────────────────
  // $86D3: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $86D3 — 内部函数 (TODO 翻译)
   * 原始: bank_11.asm, CDL code 标记
   */
  private fn_86D3(): void {
    // TODO: 翻译自 bank_11.asm $86D3
  }

}