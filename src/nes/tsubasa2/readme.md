//天使之翼2的H5版本
//去除指令6502，cpu改成bus控制跳转。//mmc3 直接写对应的模式，但是去除bank切换的功能，直接改成方法调用。读取对应数据等。//rom数据chr 变成可视化的tiles，每个tiles一个固定id



src\asm
src\core 根据这两个翻译成src\game，java语法的ts版本移植版，内容移植素材移植，代码逻辑要改成高级语言，不要地址不要6502汇编语言，出迭代方案，开始创建页面并迭代开发。保证每个版本运行正常。不编造，要与原始游戏内容一致。要求每个版本的渲染和音频调用，界面流转链路都是真实游戏的行为。