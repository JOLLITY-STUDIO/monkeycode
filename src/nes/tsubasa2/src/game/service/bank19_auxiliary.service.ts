/**
 * Bank 19 Service — Auxiliary Logic & Data (骨架)
 *
 * 数据已直接 import `rom-data/prg-bank-19.ts` (Bank #0x13 = 19), 无 MMC3 切换。
 * PRG offset: 0x026010-0x02800F
 *
 * 本文件为 code 翻译骨架 (来源: _tmp_bzk_out/bank_19.asm, CDL C 标记)。
 *
 * 原始入口 (跳转表):
 *   (无跳转表, 直接执行 $9000)
 *
 * code 段 (5):
 *   $9000-$9163 (356 B)
 *   $91A9-$922A (130 B)
 *   $9238-$9243 (12 B)
 *   $92A8-$930F (104 B)
 *   $9335-$944D (281 B)
 *
 * 本地函数 (被 JSR 调用, 6):
 *   $B043×1 $B127×1 $B15A×1 $B160×1 $B2A6×1 $B406×2
 *
 * 本地 JMP 目标:
 *   $B02D×2 $B0AF×2 $B20F×1 $B2F7×1 $B339×1 $B3CB×1 $B3FA×1
 */

import { DataStore } from '../data/DataStore';
import PRG_BANK_19 from "../data/prg-bank-19";

// ═══════════════════════════════════════════════════════════════
// Bank19Service
// ═══════════════════════════════════════════════════════════════

export class Bank19Service {
  constructor(private _store: DataStore) {}

  // ── 数据访问 (原始字节, 未结构化) ──

  /** 读取本 bank 内地址 addr 的原始字节 (addr: $9000-$944D) */
  readByte(addr: number): number {
    return PRG_BANK_19[addr - 0x8000] ?? 0xFF;
  }

  /** 读取本 bank 内 16bit 小端数值 */
  readU16(addr: number): number {
    return this.readByte(addr) | (this.readByte(addr + 1) << 8);
  }

  get store(): DataStore { return this._store; }

  // ──────────────────────────────────────────────
  // $9000: 入口
  // ──────────────────────────────────────────────

  /**
   * $9000 — 入口 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  entry_9000(): void {
    // TODO: 翻译自 bank_19.asm $9000
  }

  // ──────────────────────────────────────────────
  // $91A9: 入口
  // ──────────────────────────────────────────────

  /**
   * $91A9 — 入口 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  entry_91A9(): void {
    // TODO: 翻译自 bank_19.asm $91A9
  }

  // ──────────────────────────────────────────────
  // $9238: 入口
  // ──────────────────────────────────────────────

  /**
   * $9238 — 入口 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  entry_9238(): void {
    // TODO: 翻译自 bank_19.asm $9238
  }

  // ──────────────────────────────────────────────
  // $92A8: 入口
  // ──────────────────────────────────────────────

  /**
   * $92A8 — 入口 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  entry_92A8(): void {
    // TODO: 翻译自 bank_19.asm $92A8
  }

  // ──────────────────────────────────────────────
  // $9335: 入口
  // ──────────────────────────────────────────────

  /**
   * $9335 — 入口 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  entry_9335(): void {
    // TODO: 翻译自 bank_19.asm $9335
  }

  // ──────────────────────────────────────────────
  // $B043: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $B043 — 内部函数 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  private fn_B043(): void {
    // TODO: 翻译自 bank_19.asm $B043
  }

  // ──────────────────────────────────────────────
  // $B127: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $B127 — 内部函数 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  private fn_B127(): void {
    // TODO: 翻译自 bank_19.asm $B127
  }

  // ──────────────────────────────────────────────
  // $B15A: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $B15A — 内部函数 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  private fn_B15A(): void {
    // TODO: 翻译自 bank_19.asm $B15A
  }

  // ──────────────────────────────────────────────
  // $B160: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $B160 — 内部函数 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  private fn_B160(): void {
    // TODO: 翻译自 bank_19.asm $B160
  }

  // ──────────────────────────────────────────────
  // $B2A6: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $B2A6 — 内部函数 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  private fn_B2A6(): void {
    // TODO: 翻译自 bank_19.asm $B2A6
  }

  // ──────────────────────────────────────────────
  // $B406: 内部函数
  // ──────────────────────────────────────────────

  /**
   * $B406 — 内部函数 (TODO 翻译)
   * 原始: bank_19.asm, CDL code 标记
   */
  private fn_B406(): void {
    // TODO: 翻译自 bank_19.asm $B406
  }

}